def context_reduction(query: str) -> str:
    return """Сократи следующий текст/диалог до ключевых фактов и решений, сохранив:
    - Главные договорённости и выводы
    - Важные числа, даты, имена
    - Нерешённые вопросы
    - Текущий статус задачи

    Убери: приветствия, рассуждения, повторы, промежуточные варианты.

    Формат: сжатый список или краткое саммари.

    ТЕКСТ ДЛЯ СОКРАЩЕНИЯ:

    """ + query



# ==================== Phase 1: Task Classification ====================

CLASSIFICATION_SYSTEM_PROMPT = """Ты - классификатор задач. Определи тип ПОСЛЕДНЕГО запроса пользователя.

ТИПЫ ЗАДАЧ:
1 - Задача коммивояжёра (TSP): маршрут, города, расстояния
2 - Максимальная клика: граф, вершины, рёбра, клика
3 - Задача о рюкзаке: предметы, вес, ценность, вместимость
4 - Арифметика: вычисления, математические выражения
5 - Клика максимального веса: взвешенный граф, вершины, рёбра с весами, клика с максимальной суммой весов рёбер
6 - Планирование производства: заказы, станки, время выполнения, матрица переналадки между заказами, ценность заказов на станках, распределение
7 - Мультирюкзак: предметы с несколькими ограничениями (вес, объём и т.д.), несколько лимитов, ценность каждого предмета
8 - Двумерное замощение (тайлинг): замостить прямоугольную область заданными прямоугольниками, возможны перегородки и забаненные клетки

СПЕЦИАЛЬНЫЕ КОДЫ:
0 - Обычный разговор (приветствие, вопрос, объяснение, диалог, благодарность, прощание)
-1 - Задача определена, но данных недостаточно

ВАЖНО — ПРОВЕРКА ДОСТАТОЧНОСТИ ДАННЫХ:
Перед тем как вернуть код задачи (1, 2, 3, 4, 5, 6, 7 или 8), ВНИМАТЕЛЬНО проверь, содержит ли запрос ВСЕ необходимые данные для решения:

- **TSP (1)**: нужна ПОЛНАЯ матрица расстояний или ВСЕ попарные расстояния между городами. Если указаны города, но не все расстояния — отвечай -1 1.
- **Максимальная клика (2)**: нужна матрица смежности или ПОЛНЫЙ список рёбер графа с указанием всех вершин. Граф НЕВЗВЕШЕННЫЙ (рёбра есть или нет). Если граф описан неполно — отвечай -1 2.
- **Рюкзак (3)**: нужны вместимость рюкзака И список предметов с весом И ценностью каждого. Если не указана вместимость, или у предметов нет веса/ценности — отвечай -1 3.
- **Арифметика (4)**: нужно конкретное математическое выражение для вычисления.
- **Клика максимального веса (5)**: нужна матрица смежности с ВЕСАМИ рёбер (числа > 0 означают ребро с таким весом, 0 — нет ребра) или ПОЛНЫЙ список рёбер с весами. Граф ВЗВЕШЕННЫЙ. Если граф описан неполно или не указаны веса рёбер — отвечай -1 5.
- **Планирование производства (6)**: нужна матрица времени выполнения (строки — станки, столбцы — заказы), матрица времени переналадки между заказами (JxJ, где J — число заказов) И матрица ценности заказов на станках (строки — станки, столбцы — заказы). Если не указана матрица выполнения, или нет времени переналадки, или нет ценности заказов — отвечай -1 6.
- **Мультирюкзак (7)**: нужны ВСЕ лимиты (ограничения) по каждому типу ресурса И список предметов, где для КАЖДОГО предмета указаны ценность И значения по КАЖДОМУ типу ограничения (вес, объём и т.д.). Если не указаны лимиты, или у предметов не указаны все параметры — отвечай -1 7.
- **Двумерное замощение (8)**: нужен список прямоугольников (ширина и длина каждого) И размеры области для замощения (ширина и длина). Перегородки (separations) и забаненные клетки (banned) — опциональны. Если не указаны размеры области или список прямоугольников — отвечай -1 8.

РАЗЛИЧИЕ МЕЖДУ ЗАДАЧАМИ 3 И 7:
- Задача 3 (рюкзак) — ОДИН ограничитель (вес), одна вместимость. У каждого предмета: вес и ценность.
- Задача 7 (мультирюкзак) — НЕСКОЛЬКО ограничителей (вес, объём, количество и т.д.), несколько лимитов. У каждого предмета: ценность и значение по каждому ограничителю.
- Если пользователь упоминает несколько типов ограничений (вес И объём, вес И размер, и т.д.) — это задача 7.
- Если у предмета только один ограничитель (вес) — это задача 3.

РАЗЛИЧИЕ МЕЖДУ ЗАДАЧАМИ 2 И 5:
- Задача 2 (максимальная клика) — граф НЕВЗВЕШЕННЫЙ, рёбра только 0 или 1, цель — найти клику с наибольшим числом вершин.
- Задача 5 (клика максимального веса) — граф ВЗВЕШЕННЫЙ, рёбра имеют числовые веса, цель — найти клику с максимальной суммой весов рёбер.
- Если пользователь упоминает веса рёбер, стоимости связей или числовые значения на рёбрах — это задача 5.
- Если пользователь говорит просто о наличии/отсутствии рёбер без весов — это задача 2.

Если пользователь просит решить задачу, но НЕ предоставил достаточно данных — ВСЕГДА отвечай -1 с номером задачи. НЕ пытайся угадать недостающие данные.

ДРУГИЕ ПРАВИЛА:
- Если в истории диалога уже есть "API response:" с результатом решения задачи, и пользователь НЕ просит решить новую задачу — отвечай 0 (это обычный разговор, нужно показать результат).
- Отвечай ТОЛЬКО числом (например: 4)
- Если нужны уточнения для задачи, отвечай: -1 <номер_задачи> (например: -1 3)
- Никакого дополнительного текста, только число!

ПРИМЕРЫ:

Запрос: "Вычисли 5 + 3"
Ответ: 4

Запрос: "Что такое задача коммивояжёра?"
Ответ: 0

Запрос: "Реши задачу о рюкзаке"
Ответ: -1 3

Запрос: "Реши задачу о рюкзаке. Предметы: книга 2кг, ноутбук 3кг"
Ответ: -1 3
(нет вместимости рюкзака и ценности предметов)

Запрос: "Найди максимальную клику в графе с 5 вершинами"
Ответ: -1 2
(нет информации о рёбрах графа)

Запрос: "Найди кратчайший маршрут через города A, B, C с расстояниями A-B=10, A-C=15, B-C=20"
Ответ: 1

Запрос: "Найди кратчайший маршрут через города A, B, C. A-B=10, A-C=15"
Ответ: -1 1
(не указано расстояние B-C)

Запрос: "Найди клику максимального веса в графе. Вершины: A, B, C, D. Рёбра: A-B(вес 5), A-C(вес 3), B-C(вес 7), C-D(вес 2)"
Ответ: 5

Запрос: "Найди клику с максимальным весом рёбер в графе с 4 вершинами"
Ответ: -1 5
(нет информации о рёбрах и их весах)

Запрос: "Распредели 3 заказа по 2 станкам. Время выполнения: заказ1-станок1=5, заказ1-станок2=3, заказ2-станок1=4, заказ2-станок2=6, заказ3-станок1=2, заказ3-станок2=7. Переналадка между заказами: 1→2=2, 1→3=1, 2→1=2, 2→3=3, 3→1=1, 3→2=3. Ценность: заказ1-станок1=10, заказ1-станок2=8, заказ2-станок1=15, заказ2-станок2=20, заказ3-станок1=12, заказ3-станок2=9"
Ответ: 6

Запрос: "Оптимизируй производство для 4 заказов"
Ответ: -1 6
(нет матрицы времени выполнения, времени переналадки и ценности заказов)

Запрос: "Мультирюкзак. Лимиты: вес 10, объём 8. Предметы: A(ценность 5, вес 2, объём 3), B(ценность 8, вес 4, объём 2), C(ценность 3, вес 1, объём 4)"
Ответ: 7

Запрос: "Реши задачу мультирюкзака для 5 предметов"
Ответ: -1 7
(нет лимитов и параметров предметов)

Запрос: "Замости область 4x3 прямоугольниками 2x1 и 1x1. Клетка (0,0) забанена. Перегородка между (1,0) и (1,1)"
Ответ: 8

Запрос: "Замости прямоугольную область плитками"
Ответ: -1 8
(нет размеров области и списка прямоугольников)

Запрос: "User: Вычисли 2+2\\nAPI response: 2 + 2 = 4"
Ответ: 0
"""


def get_task_classification_prompt(query: str) -> str:
    """Generate Phase 1 prompt for task type classification.

    The model should return ONLY a number (0, 1, 2, 3, 4, 5, 6, 7, 8, or -1 N).
    """
    return f"""{CLASSIFICATION_SYSTEM_PROMPT}

ИСТОРИЯ ДИАЛОГА:
{query}

Определи тип последнего запроса пользователя. Ответь только числом:"""


# ==================== Phase 2: Data Extraction ====================

def get_data_extraction_prompt(query: str, task_id: int) -> str:
    """Generate Phase 2 prompt for extracting task-specific data.

    Returns a prompt tailored to the specific task type so the model
    outputs data in a simple, easy-to-parse text format (no JSON).
    """
    if task_id == 4:
        return _get_arithmetic_extraction_prompt(query)
    elif task_id == 1:
        return _get_tsp_extraction_prompt(query)
    elif task_id == 2:
        return _get_clique_extraction_prompt(query)
    elif task_id == 3:
        return _get_knapsack_extraction_prompt(query)
    elif task_id == 5:
        return _get_max_weight_clique_extraction_prompt(query)
    elif task_id == 6:
        return _get_production_scheduling_extraction_prompt(query)
    elif task_id == 7:
        return _get_multi_knapsack_extraction_prompt(query)
    elif task_id == 8:
        return _get_tiling_2d_extraction_prompt(query)
    else:
        return f"Извлеки данные для задачи типа {task_id} из запроса:\n{query}"


def _get_arithmetic_extraction_prompt(query: str) -> str:
    """Extract arithmetic expression from user query."""
    return f"""Ты — экстрактор данных. Извлеки математическое выражение из запроса.

СТРОГИЕ ПРАВИЛА:
- Выведи ТОЛЬКО математическое выражение — ОДНУ строку
- Используй операторы: + - * / ** % //
- НЕ вычисляй результат
- НЕ пиши пояснений, шагов, слов, заголовков
- НЕ используй markdown-форматирование
- ЗАПРЕЩЕНО писать что-либо кроме самого выражения

ПРИМЕРЫ:

Запрос: "Вычисли 2 плюс 2 умножить на 2"
2 + 2 * 2

Запрос: "Сколько будет (15 + 5) разделить на 4?"
(15 + 5) / 4

ЗАПРОС:
{query}
"""


def _get_tsp_extraction_prompt(query: str) -> str:
    """Extract distance matrix for TSP."""
    return f"""Ты — экстрактор данных. Извлеки матрицу расстояний между городами из запроса.

СТРОГИЕ ПРАВИЛА:
- Выведи ТОЛЬКО матрицу — числа через пробелы, каждая строка на новой строке
- Матрица симметричная, диагональ = 0
- НЕ пиши пояснений, шагов, слов, заголовков, названий городов
- НЕ используй markdown-форматирование (никаких ```, #, *, $)
- ЗАПРЕЩЕНО писать что-либо кроме чисел матрицы

ПРИМЕРЫ:

Запрос: "Города A, B, C. Расстояния: A-B=10, A-C=15, B-C=20"
0 10 15
10 0 20
15 20 0

Запрос: "4 города: X, Y, Z, W. X-Y=5, X-Z=8, X-W=3, Y-Z=6, Y-W=7, Z-W=9"
0 5 8 3
5 0 6 7
8 6 0 9
3 7 9 0

ЗАПРОС:
{query}
"""


def _get_clique_extraction_prompt(query: str) -> str:
    """Extract adjacency matrix for Max Clique."""
    return f"""Ты — экстрактор данных. Извлеки матрицу смежности графа из запроса.

СТРОГИЕ ПРАВИЛА:
- Выведи ТОЛЬКО матрицу — числа 0 и 1 через пробелы, каждая строка на новой строке
- Матрица симметричная, диагональ = 0
- 1 = есть ребро, 0 = нет ребра
- НЕ пиши пояснений, шагов, слов, заголовков, названий вершин
- НЕ используй markdown-форматирование (никаких ```, #, *, $)
- ЗАПРЕЩЕНО писать что-либо кроме чисел матрицы

ПРИМЕРЫ:

Запрос: "Вершины A, B, C. Рёбра: A-B, B-C, A-C"
0 1 1
1 0 1
1 1 0

Запрос: "Граф: вершины 1,2,3,4. Рёбра: 1-2, 1-3, 2-3, 3-4"
0 1 1 0
1 0 1 0
1 1 0 1
0 0 1 0

ЗАПРОС:
{query}
"""


def _get_knapsack_extraction_prompt(query: str) -> str:
    """Extract knapsack data (capacity + items)."""
    return f"""Ты — экстрактор данных. Извлеки данные задачи о рюкзаке из запроса.

СТРОГИЕ ПРАВИЛА:
- Первая строка: вместимость рюкзака (ОДНО число)
- Следующие строки: вес ценность (ДВА числа через пробел на каждый предмет)
- НЕ пиши пояснений, шагов, слов, заголовков, названий предметов
- НЕ используй markdown-форматирование (никаких ```, #, *, $)
- ЗАПРЕЩЕНО писать что-либо кроме чисел

ПРИМЕРЫ:

Запрос: "Рюкзак 10 кг. Предметы: книга 2 кг 5 руб, ноутбук 3 кг 10 руб"
10
2 5
3 10

Запрос: "Вместимость 15. Вещи: A(вес 3, цена 4), B(вес 5, цена 8), C(вес 7, цена 10)"
15
3 4
5 8
7 10

ЗАПРОС:
{query}
"""


def _get_max_weight_clique_extraction_prompt(query: str) -> str:
    """Extract weighted adjacency matrix for Max Weight Clique."""
    return f"""Ты — экстрактор данных. Извлеки взвешенную матрицу смежности графа из запроса.

СТРОГИЕ ПРАВИЛА:
- Выведи ТОЛЬКО матрицу — числа через пробелы, каждая строка на новой строке
- Матрица симметричная, диагональ = 0
- Число > 0 означает ребро с таким весом, 0 = нет ребра
- НЕ пиши пояснений, шагов, слов, заголовков, названий вершин
- НЕ используй markdown-форматирование (никаких ```, #, *, $)
- ЗАПРЕЩЕНО писать что-либо кроме чисел матрицы

ПРИМЕРЫ:

Запрос: "Вершины A, B, C. Рёбра: A-B(вес 5), B-C(вес 3), A-C(вес 7)"
0 5 7
5 0 3
7 3 0

Запрос: "Граф: вершины 1,2,3,4. Рёбра: 1-2(вес 10), 1-3(вес 6), 2-3(вес 8), 3-4(вес 4)"
0 10 6 0
10 0 8 0
6 8 0 4
0 0 4 0

ЗАПРОС:
{query}
"""


def _get_production_scheduling_extraction_prompt(query: str) -> str:
    """Extract production scheduling data (3 matrices: completion times, setup times, job values)."""
    return f"""Ты — экстрактор данных. Извлеки данные задачи планирования производства из запроса.

СТРОГИЕ ПРАВИЛА:
- Блок 1 — МАТРИЦА времени выполнения (MxJ): строки — СТАНКИ (M штук), столбцы — РАБОТЫ (J штук). В клетке (i,j) — сколько времени станок i выполняет работу j. Числа через пробелы, каждая строка на новой строке.
- После матрицы — пустая строка
- Блок 2 — МАТРИЦА времени переналадки (JxJ): квадратная матрица. В клетке (i,j) — время переключения ЛЮБОГО станка с работы i на работу j. Диагональ = 0. Числа через пробелы, каждая строка на новой строке.
- После матрицы — пустая строка
- Блок 3 — МАТРИЦА ценности (MxJ): строки — СТАНКИ, столбцы — РАБОТЫ. В клетке (i,j) — сколько денег мы получаем за выполнение работы j на станке i. Числа через пробелы, каждая строка на новой строке.
- Если пользователь указал ценность заказов как ОДИН вектор (одно число на заказ), продублируй его для каждого станка (все строки одинаковые).
- Если пользователь указал переналадку как ОДИН вектор (одно число на заказ), построй матрицу JxJ, где в каждой клетке (i,j) при i≠j стоит значение для заказа j (время настройки НА заказ j), а диагональ = 0.
- НЕ пиши пояснений, шагов, слов, заголовков
- НЕ используй markdown-форматирование (никаких ```, #, *, $)
- ЗАПРЕЩЕНО писать что-либо кроме чисел

ПРИМЕРЫ:

Запрос: "3 заказа, 2 станка. Время: заказ1-станок1=5, заказ1-станок2=3, заказ2-станок1=4, заказ2-станок2=6, заказ3-станок1=2, заказ3-станок2=7. Переналадка: 1→2=2, 1→3=1, 2→1=2, 2→3=3, 3→1=1, 3→2=3. Ценность: заказ1-станок1=10, заказ1-станок2=8, заказ2-станок1=15, заказ2-станок2=20, заказ3-станок1=12, заказ3-станок2=9"
5 4 2
3 6 7

0 2 1
2 0 3
1 3 0

10 15 12
8 20 9

Запрос: "2 заказа на 3 станках. Выполнение: A на станке1=10, станке2=8, станке3=12; B на станке1=6, станке2=9, станке3=5. Переналадка: A=3, B=2. Ценность: A=50, B=30"
10 6
8 9
12 5

0 2
3 0

50 30
50 30
50 30

ЗАПРОС:
{query}
"""


def _get_multi_knapsack_extraction_prompt(query: str) -> str:
    """Extract multi-knapsack data (capabilities + items with value and multiple weights)."""
    return f"""Ты — экстрактор данных. Извлеки данные задачи мультирюкзака из запроса.

СТРОГИЕ ПРАВИЛА:
- Первая строка: лимиты (ограничения) через пробелы — по одному числу на каждый тип ограничения
- Следующие строки: по одной строке на каждый предмет — первое число = ценность, далее через пробелы значения по каждому ограничителю (в том же порядке, что и лимиты)
- НЕ пиши пояснений, шагов, слов, заголовков, названий предметов
- НЕ используй markdown-форматирование (никаких ```, #, *, $)
- ЗАПРЕЩЕНО писать что-либо кроме чисел

ПРИМЕРЫ:

Запрос: "Лимиты: вес 10, объём 8. Предметы: A(ценность 5, вес 2, объём 3), B(ценность 8, вес 4, объём 2), C(ценность 3, вес 1, объём 4)"
10 8
5 2 3
8 4 2
3 1 4

Запрос: "Ограничения: вес до 5, размер до 5. Предметы: 1(цена 1, вес 1, размер 2), 2(цена 2, вес 2, размер 3), 3(цена 3, вес 3, размер 4), 4(цена 4, вес 4, размер 1)"
5 5
1 1 2
2 2 3
3 3 4
4 4 1

ЗАПРОС:
{query}
"""


def _get_tiling_2d_extraction_prompt(query: str) -> str:
    """Extract 2D tiling data (rectangles, area dimensions, optional banned/separations)."""
    return f"""Ты — экстрактор данных. Извлеки данные задачи двумерного замощения из запроса.

СТРОГИЕ ПРАВИЛА:
- Блок 1 — РАЗМЕРЫ ОБЛАСТИ: одна строка с двумя числами через пробел — ширина и длина области для замощения
- Блок 2 — ПРЯМОУГОЛЬНИКИ: по одной строке на каждый прямоугольник — два числа через пробел (ширина и длина)
- После прямоугольников — пустая строка
- Блок 3 (ОПЦИОНАЛЬНО) — ПЕРЕГОРОДКИ: слово SEPARATIONS на отдельной строке, затем по одной строке на каждую перегородку — 4 числа через пробел (row1 col1 row2 col2), описывающие две клетки, между которыми перегородка. Если перегородок нет — пропусти этот блок.
- Блок 4 (ОПЦИОНАЛЬНО) — ЗАБАНЕННЫЕ КЛЕТКИ: слово BANNED на отдельной строке, затем по одной строке на каждую забаненную клетку — 2 числа через пробел (row col). Если забаненных клеток нет — пропусти этот блок.
- НЕ пиши пояснений, шагов, слов (кроме SEPARATIONS и BANNED), заголовков
- НЕ используй markdown-форматирование (никаких ```, #, *, $)
- ЗАПРЕЩЕНО писать что-либо кроме чисел и ключевых слов SEPARATIONS/BANNED

ПРИМЕРЫ:

Запрос: "Замости область 4x3 прямоугольниками 2x1 и 1x1"
4 3
2 1
1 1

Запрос: "Замости область 5x4 плитками 2x1, 1x2, 1x1. Клетка (0,0) забанена. Перегородка между (1,0) и (1,1)"
5 4
2 1
1 2
1 1

SEPARATIONS
1 0 1 1

BANNED
0 0

Запрос: "Область 3x3, плитки 2x1 и 1x1. Забанены клетки (0,0) и (2,2). Перегородки: между (0,1)-(0,2) и между (1,0)-(2,0)"
3 3
2 1
1 1

SEPARATIONS
0 1 0 2
1 0 2 0

BANNED
0 0
2 2

ЗАПРОС:
{query}
"""


# ==================== Clarification & Conversation ====================

def get_clarification_prompt(query: str, task_id: int) -> str:
    """Generate prompt to ask user for missing data."""
    task_names = {
        1: "задачи коммивояжёра (TSP)",
        2: "задачи о максимальной клике",
        3: "задачи о рюкзаке",
        4: "арифметического вычисления",
        5: "задачи о клике максимального веса",
        6: "задачи планирования производства",
        7: "задачи мультирюкзака",
        8: "задачи двумерного замощения (тайлинга)"
    }

    task_requirements = {
        1: "Для решения задачи коммивояжёра необходимы: список всех городов и ВСЕ попарные расстояния между ними (матрица расстояний). Для N городов нужно N*(N-1)/2 расстояний.",
        2: "Для решения задачи о максимальной клике необходимы: список всех вершин графа и ПОЛНЫЙ список рёбер (какие вершины соединены между собой), либо матрица смежности.",
        3: "Для решения задачи о рюкзаке необходимы: вместимость рюкзака (максимальный вес) и список предметов, где для КАЖДОГО предмета указаны вес И ценность.",
        4: "Для вычисления необходимо конкретное математическое выражение.",
        5: "Для решения задачи о клике максимального веса необходимы: список всех вершин графа и ПОЛНЫЙ список рёбер с ВЕСАМИ (числовое значение веса каждого ребра), либо взвешенная матрица смежности. Для N вершин нужно указать все существующие рёбра и их веса.",
        6: "Для решения задачи планирования производства необходимы: 1) матрица времени выполнения (строки — станки, столбцы — заказы), 2) матрица времени переналадки между заказами (JxJ, время перехода от одного заказа к другому), 3) матрица ценности заказов на станках (строки — станки, столбцы — заказы). Если переналадка задана одним числом на заказ — это допустимо. Если ценность задана одним числом на заказ — это тоже допустимо.",
        7: "Для решения задачи мультирюкзака необходимы: 1) лимиты (ограничения) по каждому типу ресурса (например, максимальный вес, максимальный объём), 2) список предметов, где для КАЖДОГО предмета указаны ценность И значения по КАЖДОМУ типу ограничения.",
        8: "Для решения задачи двумерного замощения необходимы: 1) размеры прямоугольной области для замощения (ширина и длина), 2) список прямоугольников-плиток (ширина и длина каждого). Опционально: забаненные клетки (координаты клеток, которые нельзя покрывать) и перегородки (пары клеток, которые не могут принадлежать одному прямоугольнику)."
    }

    task_name = task_names.get(task_id, f"задачи типа {task_id}")
    requirements = task_requirements.get(task_id, "Необходимы полные входные данные для задачи.")

    return f"""Пользователь хочет решить {task_name}, но предоставил недостаточно данных.

ЗАПРОС ПОЛЬЗОВАТЕЛЯ:
{query}

ТРЕБОВАНИЯ К ДАННЫМ:
{requirements}

Внимательно проанализируй запрос пользователя и определи, каких именно данных не хватает. Вежливо попроси пользователя предоставить КОНКРЕТНЫЕ недостающие данные. Перечисли, что именно нужно дополнить. Если часть данных уже предоставлена — подтверди это и укажи только недостающее.

Твой ответ:"""


def get_conversational_prompt(query: str) -> str:
    """Generate prompt for a regular conversational response."""
    return f"""Ты — дружелюбный ассистент, специализирующийся на решении NP-полных задач методом QUBO.

Если пользователь спрашивает о методе решения, объясни:
- Задачи (коммивояжёр, максимальная клика, клика максимального веса, рюкзак, мультирюкзак, планирование производства, двумерное замощение) решаются методом QUBO (Quadratic Unconstrained Binary Optimization).
- QUBO сводит задачу к минимизации квадратичной функции f(x) = xᵀQx с бинарными переменными (0 или 1).
- Ограничения задачи кодируются как штрафные слагаемые в целевой функции.
- Минимизация выполняется методом симуляции отжига (simulated annealing) — эвристическим алгоритмом, имитирующим процесс охлаждения металла.
- Результат может быть приближённым, но близким к оптимальному.

Если в истории диалога есть результат решения задачи (после "API response:"), красиво оформи и объясни этот результат пользователю.

ИСТОРИЯ ДИАЛОГА:
{query}

Твой ответ:"""


def get_final_response_prompt(query: str, task_name: str, task_result: str) -> str:
    """Generate prompt to present the solved task result to the user.

    After the solver produces a result, we ask the model to wrap it in
    a friendly, human-readable message.
    """
    return f"""Ты — дружелюбный ассистент. Пользователь попросил решить задачу, и она уже решена.

Задача: {task_name}

ГОТОВЫЙ РЕЗУЛЬТАТ РЕШЕНИЯ (используй ИМЕННО эти данные, НЕ придумывай свои):
{task_result}

ИСТОРИЯ ДИАЛОГА:
{query}

## Метод решения
Задача была решена методом QUBO (Quadratic Unconstrained Binary Optimization) — квадратичной оптимизации с бинарными переменными. Исходная задача была сведена к минимизации квадратичной функции f(x) = xᵀQx, где каждая переменная принимает значение 0 или 1. Ограничения задачи закодированы как штрафные слагаемые. Оптимизация выполнена методом симуляции отжига (simulated annealing). Результат может быть приближённым.

## СТРОГИЕ ИНСТРУКЦИИ
- Представь результат решения задачи пользователю, используя КОНКРЕТНЫЕ числа и данные из раздела «ГОТОВЫЙ РЕЗУЛЬТАТ РЕШЕНИЯ» выше.
- ЗАПРЕЩЕНО использовать заглушки вроде «(здесь укажите...)», «(см. результат)» или подобные плейсхолдеры. Все данные уже есть — используй их.
- Если в результате есть маршрут — перечисли его полностью с номерами/названиями.
- Если в результате есть выбранные предметы — перечисли их.
- Если в результате есть вершины клики — перечисли их.
- Если в результате есть распределение работ по станкам — ВНИМАТЕЛЬНО: СТАНОК — это оборудование (машина), РАБОТА — это задание (заказ). Для каждого СТАНКА перечисли его очередь РАБОТ в указанном порядке. НЕ путай станки и работы местами!
- Кратко упомяни, что задача решена методом QUBO с использованием симуляции отжига, и что результат может быть приближённым.
- НЕ решай задачу заново — просто оформи готовый результат.

Твой ответ:"""
